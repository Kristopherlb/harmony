/**
 * @golden/workbench-server
 * Secure Workbench Session server (launch + embedded) for Swagger UI and GraphQL querying.
 */
export {
  createWorkbenchServer,
  type WorkbenchServer,
  type WorkbenchServerOptions,
} from './src/server.js';

