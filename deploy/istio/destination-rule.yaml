apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: harmony-mcp
spec:
  host: harmony-mcp
  subsets:
    - name: stable
      labels:
        version: stable
    - name: canary
      labels:
        version: canary
